# Installation

Pre-built packages are not yet available. These guides are prepared for when releases are published.

To run PhantomLink now, build from source:

```bash
git clone https://github.com/ghostkellz/phantomlink.git
cd phantomlink
cargo build --release
./target/release/phantomlink
```
